import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Transfer Page",
};

export default async function TransferPage() {
  return (
    <div>
      <h1>Transfer Page</h1>
    </div>
  );
}
